{
  "name": "@ctf-blockchain-infra/examples",
  "version": "0.0.0",
  "private": true,
  "description": "Shared deps for example solver scripts. Run `npm install` here once; every example/*/solver/*.js then resolves `ethers` and the SDK.",
  "type": "module",
  "dependencies": {
    "ethers": "^6.13.4"
  }
}
